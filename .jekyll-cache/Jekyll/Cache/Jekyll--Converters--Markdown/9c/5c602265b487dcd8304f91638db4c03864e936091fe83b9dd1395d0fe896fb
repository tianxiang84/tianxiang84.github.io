I"<p><a href="https://en.wikipedia.org/wiki/Prefix_sum">Prefix Sum</a> problem is to compute the sum of all the previous elements in an array. Specifically, <em>exclusive prefix sum</em> would compute all the strictly previous (self-exclusive) elements. For example,</p>
:ET